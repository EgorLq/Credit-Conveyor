# Logging
logging:
  config: classpath:log4j2.xml
  level:
    root: info

# Port
server:
  port: 8083

# Application
documentFolder: ${DOCUMENT_FOLDER:D:\\YandexDisk\\bank_doc_folder\\}

# Kafka
spring:
  kafka:
    bootstrap-servers: ${KAFKA_SERVERS:localhost:29092}
    consumer:
      group-id: dossierGroup
